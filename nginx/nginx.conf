user www-data;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;

    # Gzip
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1000;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # Rate limiting (исправлено: r/h не поддерживается)
    limit_req_zone $binary_remote_addr zone=api:10m rate=30r/m;
    limit_req_zone $binary_remote_addr zone=comment:10m rate=2r/m;

    # Upstreams (исправлено для нативного nginx)
    upstream backend {
        server 127.0.0.1:8000;
        keepalive 16;
    }

    upstream frontend {
        server 127.0.0.1:3000;
        keepalive 16;
    }

    # HTTP → HTTPS redirect
    server {
        listen 80;
        server_name lemanas.ru www.lemanas.ru;
        return 301 https://lemanas.ru$request_uri;
    }

    # Main HTTPS server
    server {
        listen 443 ssl http2;
        server_name lemanas.ru www.lemanas.ru;

        # SSL certificates
        ssl_certificate /etc/letsencrypt/live/lemanas.ru/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/lemanas.ru/privkey.pem;

        # Modern SSL configuration
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305';
        ssl_prefer_server_ciphers off;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 1d;
        ssl_session_tickets off;

        # OCSP Stapling
        ssl_stapling on;
        ssl_stapling_verify on;
        resolver 8.8.8.8 8.8.4.4 valid=300s;
        resolver_timeout 5s;

        # Security headers
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "strict-origin-when-cross-origin" always;

        client_max_body_size 10M;

        # Блокировка сканеров уязвимостей
        location ~* ^/(wp-admin|wp-login|xmlrpc\.php|administrator|components|modules|plugins|templates|media/system) {
            access_log /var/log/nginx/blocked-scans.log;
            return 403;
        }

        location ~* \.(env|git|sql|bak|old|backup|zip|tar\.gz|config\.php|wp-config)$ {
            access_log /var/log/nginx/blocked-scans.log;
            return 404;
        }

        # Фавикон — точное совпадение
        location = /favicon.ico {
            alias /opt/lemanas/frontend/public/favicon.ico;
            access_log off;
            log_not_found off;
            expires 1y;
            add_header Cache-Control "public, immutable";
            add_header Content-Type image/x-icon;
        }

        # Next.js static assets
        location ^~ /_next/static/ {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
            proxy_buffering off;
            proxy_cache_bypass $http_upgrade;
        }

        location ^~ /_next/ {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_read_timeout 90s;
        }

        # Статические файлы Django
        location /static/ {
            alias /opt/lemanas/backend/staticfiles/;
            expires 30d;
            add_header Cache-Control "public, immutable";
            access_log off;
            try_files $uri =404;
        }

        # Медиа-файлы
        location /media/ {
            alias /opt/lemanas/backend/media/;
            expires 7d;
            add_header Cache-Control "public";
            access_log off;
            try_files $uri =404;
        }

        # Изображения и шрифты из public
        location ~* \.(ico|png|jpg|jpeg|gif|svg|webp|manifest|json|woff2?|ttf|eot|otf)$ {
            alias /opt/lemanas/frontend/public;
            expires 30d;
            add_header Cache-Control "public, immutable";
            access_log off;
            try_files $uri =404;
        }

        # SEO-файлы
        location = /robots.txt {
            alias /opt/lemanas/frontend/public/robots.txt;
            access_log off;
            log_not_found off;
            add_header Content-Type text/plain;
        }

        location = /sitemap.html {
            alias /opt/lemanas/frontend/public/sitemap.html;
            access_log off;
            add_header Content-Type text/html;
        }

        location /sitemap.xml {
            proxy_pass http://frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            add_header Content-Type application/xml;
        }

        # Django Admin
        location /admin/ {
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 60s;
            proxy_read_timeout 60s;
        }

        # Django API
        location /api/ {
            limit_req zone=api burst=20 nodelay;
            proxy_pass http://backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_connect_timeout 30s;
            proxy_read_timeout 30s;
            proxy_send_timeout 30s;
        }

        # Next.js Frontend
        location / {
            proxy_pass http://frontend;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_read_timeout 90s;
        }

        # Certbot challenge
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
            default_type "text/plain";
        }
    }
}